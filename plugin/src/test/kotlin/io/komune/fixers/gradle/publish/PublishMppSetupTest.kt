package io.komune.fixers.gradle.publish

import io.komune.fixers.gradle.plugin.publish.PublishMppSetup
import org.assertj.core.api.Assertions
import org.junit.jupiter.api.Test

internal class PublishMppSetupTest {

	@Test
	fun takeUnless() {
//		val projectName = "f2-client"
//		val values = listOf(
//			"js" to "f2-client-js",
//			"kotlinMultiplatform" to "f2-client"
//		)

//		values.forEach { (publication, expected) ->
//			val result = PublishMppSetup.getArtifactId(projectName, publication)
//			Assertions.assertThat(result).isEqualTo(expected)
//
//		}
	}
}
