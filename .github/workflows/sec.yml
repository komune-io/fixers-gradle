name: Invoke Reusable Security Workflow

on: [push]

jobs:
  call-security-workflow:
    uses: komune-io/fixers-gradle/.github/workflows/security-workflow.yml@feat/HardforkToKomune
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}